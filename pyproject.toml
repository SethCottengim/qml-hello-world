[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qml-hello-world"
version = "0.1.0"
description = "Quantum Machine Learning Hello World Project"
authors = [
    {name = "Seth C", email = "dm-me-on@linkedin.com"},
]
requires-python = ">=3.9"
dependencies = [
    "pennylane",
]

[dependency-groups]
dev = []

[tool.hatch.build.targets.wheel]
packages = ["."]
